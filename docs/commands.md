# Erymont command reference

Erymont is launched with `ery`.

## Main commands

- `ery` — open the main dashboard
- `ery panel` — panel installers
- `ery wings` — wings management
- `ery themes` — theme management
- `ery vm` — VM setup tools
- `ery tools` — utility toolbox
- `ery infra` — infrastructure installers
- `ery help` — command help
- `ery version` — version information
